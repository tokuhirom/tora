use Test::More *;
$LIBPATH.push('t/tra/import/lib/');
use IMP2;

ok(main.meta.has_method("is"));
ok(!main.meta.has_method("unknownwwwn"));
ok(IMP2.meta.has_method("F"));
ok(IMP2.meta.has_method("bar"));
is(IMP2::bar(), 1111);

done_testing;
