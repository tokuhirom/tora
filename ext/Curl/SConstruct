import os

import os
import json
from glob import glob

conf = json.loads(open('../../config.json').read())

env = Environment(
    CXXFLAGS=conf['CXXFLAGS'],
    CCFLAGS=conf['CCFLAGS'],
    LINKFLAGS=conf['LINKFLAGS'],
    CC=conf['CC'],
    CXX=conf['CXX'],
    SHLIBPREFIX=conf['SHLIBPREFIX'],
    tools=conf['TOOLS'],
)

libs = conf['LIBS']
if os.name == 'nt':
    libs += ['curldll']
else:
    libs += ['curl']

shlib = env.SharedLibrary("lib/Curl" + conf['TORA_EXTSUFFIX'], ['curl.cc'], SHLIBPREFIX='', LIBPATH=['../..'], LIBS=libs)
Default(shlib)

env.Command('test', shlib, '../../bin/tora -I lib -I ../../lib/ t/curl.tra')

installs = []
for x in os.listdir('lib/'):
    dst = conf['TORA_LIBPREFIX'] + x
    src = "lib/" + x
    installs += [env.InstallAs(dst, src)]

env.Alias('install', [shlib, installs])
