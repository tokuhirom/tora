use XSLoader;

XSLoader.load("File::Path");

__END__

File::Path
==========

Create/Remove directories recursively.

METHODS
-------

### mkpath(Str $name) : Undef

This method is same as `mkdir -p $name`.

### rmtree(Str $name) : Undef

This method is same as `rm -rf $name`.

